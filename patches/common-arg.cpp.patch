diff --git a/common/arg.cpp b/common/arg.cpp
index 772422f68..633c8407e 100644
--- a/common/arg.cpp
+++ b/common/arg.cpp
@@ -394,7 +394,8 @@ common_models_handler common_models_handler_init(const common_params & params, l
     opts.download_dflash = spec_type_draft_dflash;
     opts.download_dspark = spec_type_draft_dspark;
     opts.download_mmproj = use_mmproj && !params.no_mmproj
-                        && params.mmproj.path.empty() && params.mmproj.url.empty();
+                         && params.mmproj.path.empty() && params.mmproj.url.empty();
+    opts.download_imatrix = params.load_mode == LLAMA_LOAD_MODE_STREAMING && params.guanaco_imatrix;
 
     if (!params.model.hf_repo.empty()) {
         plan = common_download_get_hf_plan(params.model, opts);
@@ -683,6 +684,11 @@ void common_models_handler_apply(common_models_handler & handler, common_params
             }
         });
     }
+    if (!plan.imatrix.local_path.empty()) {
+        tasks.emplace_back(plan.imatrix, opts, [&]() {
+            hf_cache::finalize_file(plan.imatrix);
+        });
+    }
     if (!plan.preset.local_path.empty()) {
         tasks.emplace_back(plan.preset, opts, [&]() {
             // if HF repo is a preset repo, we simply run server in router mode with the preset.ini file
@@ -2594,6 +2600,56 @@ common_params_context common_params_parser_init(common_params & params, llama_ex
             params.load_mode = value ? LLAMA_LOAD_MODE_MMAP : LLAMA_LOAD_MODE_NONE;
         }
     ).set_env("LLAMA_ARG_MMAP"));
+    add_opt(common_arg(
+        {"-gs", "--guanaco-streaming"},
+        {"-ngs", "--no-guanaco-streaming"},
+        "DEPRECATED in favor of `--load-mode streaming`: enable Guanaco disk streaming for MoE experts: keep only a small set of hot experts resident in RAM and stream the rest from NVMe on demand. A sibling <model>.gguf.imatrix.gguf, if present, seeds the hot set before the first token.",
+        [](common_params & params, bool value) {
+            LOG_WRN("DEPRECATED: --guanaco-streaming and --no-guanaco-streaming are deprecated. use --load-mode streaming instead\n");
+            params.load_mode = value ? LLAMA_LOAD_MODE_STREAMING : LLAMA_LOAD_MODE_MMAP;
+        }
+    ).set_env("LLAMA_ARG_GUANACO_STREAMING"));
+    add_opt(common_arg(
+        {"-gm", "--guanaco-max-experts"}, "N",
+        string_format("maximum number of hot experts to keep pinned per fused tensor when Guanaco streaming is enabled. Higher keeps more experts in RAM (fewer disk reads, more RAM); lower saves RAM at the cost of more streaming. -1 = auto (default: %d)", params.guanaco_max_experts),
+        [](common_params & params, int value) {
+            params.guanaco_max_experts = value;
+        }
+    ).set_env("LLAMA_ARG_GUANACO_MAX_EXPERTS"));
+    add_opt(common_arg(
+        {"-gi", "--guanaco-io-uring"},
+        {"-ngi", "--no-guanaco-io-uring"},
+        string_format("use io_uring for asynchronous NVMe reads when Guanaco streaming is active. Disable if io_uring is unavailable or causes instability. (default: %s)", params.guanaco_io_uring ? "enabled" : "disabled"),
+        [](common_params & params, bool value) {
+            params.guanaco_io_uring = value;
+        }
+    ).set_env("LLAMA_ARG_GUANACO_IO_URING"));
+    add_opt(common_arg(
+        {"-gp", "--guanaco-pilot"},
+        {"-ngp", "--no-guanaco-pilot"},
+        string_format("cross-layer lookahead prefetch: after the router selects experts for the current layer, predict and fetch experts for the next layer in the background. (default: %s)", params.guanaco_pilot ? "enabled" : "disabled"),
+        [](common_params & params, bool value) {
+            params.guanaco_pilot = value;
+        }
+    ).set_env("LLAMA_ARG_GUANACO_PILOT"));
+    add_opt(common_arg(
+        {"-gpm", "--guanaco-pilot-mass"}, "F",
+        string_format("pilot prefetch pruning: cover at most this fraction of the cumulative (from->to) transition mass. 1.0 = read the full top-K (no pruning). (default: %.2f)", (double)params.guanaco_pilot_mass),
+        [](common_params & params, const std::string & value) {
+            float v = std::stof(value);
+            if (v > 0.0f && v <= 1.0f) {
+                params.guanaco_pilot_mass = v;
+            }
+        }
+    ).set_env("LLAMA_ARG_GUANACO_PILOT_MASS"));
+    add_opt(common_arg(
+        {"-gx", "--guanaco-imatrix"},
+        {"-ngx", "--no-guanaco-imatrix"},
+        string_format("seed hot-expert pins from a sibling <model>.imatrix.gguf file before the first token. Requires a pre-generated importance matrix. (default: %s)", params.guanaco_imatrix ? "enabled" : "disabled"),
+        [](common_params & params, bool value) {
+            params.guanaco_imatrix = value;
+        }
+    ).set_env("LLAMA_ARG_GUANACO_IMATRIX"));
     add_opt(common_arg(
         {"-dio", "--direct-io"},
         {"-ndio", "--no-direct-io"},
@@ -2610,13 +2666,15 @@ common_params_context common_params_parser_init(common_params & params, llama_ex
         "- mmap: memory-map model (if mmap disabled, slower load but may reduce pageouts if not using mlock)\n"
         "- mlock: force system to keep model in RAM rather than swapping or compressing\n"
         "- mmap+mlock: mmap + force system to keep model in RAM rather than swapping or compressing\n"
-        "- dio: use DirectIO if available\n",
+        "- dio: use DirectIO if available\n"
+        "- streaming: mmap + keep only hot MoE experts resident, stream the rest from NVMe on demand (Guanaco)\n",
         [](common_params & params, const std::string & value) {
             /**/ if (value == "none")       { params.load_mode = LLAMA_LOAD_MODE_NONE;       }
             else if (value == "mmap")       { params.load_mode = LLAMA_LOAD_MODE_MMAP;       }
             else if (value == "mlock")      { params.load_mode = LLAMA_LOAD_MODE_MLOCK;      }
             else if (value == "mmap+mlock") { params.load_mode = LLAMA_LOAD_MODE_MMAP_MLOCK; }
             else if (value == "dio")        { params.load_mode = LLAMA_LOAD_MODE_DIRECT_IO;  }
+            else if (value == "streaming")  { params.load_mode = LLAMA_LOAD_MODE_STREAMING;  }
             else { throw std::invalid_argument("invalid value"); }
         }
     ).set_env("LLAMA_ARG_LOAD_MODE"));
